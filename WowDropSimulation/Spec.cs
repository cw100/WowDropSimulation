using System;
using System.Collections.Generic;
using System.Text;

namespace WowDropSimulation
{
    class Spec
    {
        public string className;
        public string specName;

        public Spec(string className, string specName)
        {
            this.className = className;
            this.specName = specName;
        }
    }
}
